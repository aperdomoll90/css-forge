export interface ThreeJsTestPropsType {
    size?: number;
}
  
export interface SectionPropsType {
  children: any
  offset: number
  factor: number
}

export interface ThreeStatePropsType {
  sections: number,
  pages: number,
  zoom: number,
  top: any,
}